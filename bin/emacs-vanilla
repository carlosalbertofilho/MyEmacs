#!/usr/bin/env bash
# emacs-vanilla — launch Emacs with the vanilla init directory
# Usage: emacs-vanilla [EMACS-ARGS...]

set -euo pipefail

INIT_DIR="${EMACS_VANILLA_DIR:-$HOME/.config/emacs}"
exec emacs --init-directory "$INIT_DIR" "$@"
