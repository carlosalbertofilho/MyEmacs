
* Símbolos Introspectados

Guia de Referência RAG para o pacote Forge no Emacs - integração com GitHub/GitLab, Issues e Pull Requests.

* Visão Geral

#+OPTIONS: toc:2 num:t
#+FILETAGS: :RAG:DOCS:
#+DESCRIPTION: Guia de Referência RAG para o pacote Forge no Emacs - integração com GitHub/GitLab, Issues e Pull Requests.
#+LAST_MODIFIED: 2026-08-22
#+DATE: 2026-08-22
#+AUTHOR: Carlos Filho
#+TITLE: Forge Reference Guide
** forge-create-issue
- *Tipo:* Comando Interativo
- *Assinatura:* =(forge-create-issue template)=

#+begin_src text
Create a new issue for the current repository.

(fn TEMPLATE)
#+end_src

** forge-create-pullreq
- *Tipo:* Comando Interativo
- *Assinatura:* =(forge-create-pullreq source target)=

#+begin_src text
Create a new pull-request for the current repository.

(fn SOURCE TARGET)
#+end_src

** forge-list-issues
- *Tipo:* Comando Interativo
- *Assinatura:* =(forge-list-issues &optional repo)=

#+begin_src text
List issues of the current repository.
#+end_src

** forge-list-pullreqs
- *Tipo:* Comando Interativo
- *Assinatura:* =(forge-list-pullreqs &optional repo)=

#+begin_src text
List pull-requests of the current repository.
#+end_src

** forge-pull
- *Tipo:* Comando Interativo

#+begin_src text
Pull forge topics for the current repository if it is already tracked.
If the current repository is still untracked locally, or the current
repository cannot be determined, instead invoke `forge-add-repository'.
#+end_src

** forge-browse-issues
- *Tipo:* Comando Interativo

#+begin_src text
Visit the current repository's issues using a browser.
#+end_src

** forge-browse-pullreqs
- *Tipo:* Comando Interativo

#+begin_src text
Visit the current repository's pull-requests using a browser.
#+end_src
