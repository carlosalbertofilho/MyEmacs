#+TITLE: Dirvish Reference Guide
#+AUTHOR: Carlos Filho
#+DATE: 2026-08-22
#+LAST_MODIFIED: 2026-08-22
#+DESCRIPTION: Guia de Referência RAG para o gerenciador de arquivos Dirvish no Emacs.
#+FILETAGS: :RAG:DOCS:
#+OPTIONS: toc:2 num:t

* Visão Geral

Guia de Referência RAG para o gerenciador de arquivos Dirvish no Emacs.

* Símbolos Introspectados

** dirvish
- *Tipo:* Comando Interativo
- *Assinatura:* =(dirvish &optional path)=

#+begin_src text
Open PATH in a fullframe Dirvish session.
Prompt for PATH if called with \[universal-arguments], otherwise PATH
defaults to `default-directory'.

(fn &optional PATH)
#+end_src

** dirvish-dwim
- *Tipo:* Comando Interativo
- *Assinatura:* =(dirvish-dwim &optional path)=

#+begin_src text
Open PATH in a fullframe session if selected window is the only window.
Prompt for PATH if called with \[universal-arguments], otherwise PATH
defaults to `default-directory'.  If there are other windows exist in the
selected frame, the session occupies only the selected window.

(fn &optional PATH)
#+end_src

** dirvish-layout-toggle
- *Tipo:* Comando Interativo

#+begin_src text
Toggle layout of current Dirvish session.
A session with layout means it has a companion preview window and
possibly one or more parent windows.
#+end_src

** dirvish-side
- *Tipo:* Comando Interativo
- *Assinatura:* =(dirvish-side &optional path)=

#+begin_src text
Toggle a Dirvish session at the side window.

- If the current window is a side session window, hide it.
- If a side session is visible, select it.
- If a side session exists but is not visible, show it.
- If there is no side session exists, create a new one with PATH.

If called with \[universal-arguments], prompt for PATH,
otherwise it defaults to `project-current'.

(fn &optional PATH)
#+end_src

** dirvish-history-jump
- *Tipo:* Comando Interativo
- *Assinatura:* =(dirvish-history-jump . [Arg list not available until function definition is loaded.])=

#+begin_src text
Read a recently visited directory from minibuffer and revisit it.
#+end_src

** dirvish-fd
- *Tipo:* Comando Interativo
- *Assinatura:* =(dirvish-fd . [Arg list not available until function definition is loaded.])=

#+begin_src text
Run `fd' on DIR and go into Dired mode on a buffer of the output.
The command run is essentially:

  fd --color=never `dirvish-fd-switches'
     --and PATTERN [--and PATTERN1 --and PATTERN2 … ]
     --exec-batch `dirvish-fd-ls-program' `dired-listing-switches' --directory

If called with \`C-u', prompt for the target directory,
`default-directory' is used.  If prefixed with \`C-u' twice, also
prompt for the search regex PATTERN as a comma separated list.

(fn DIR PATTERN)
#+end_src
