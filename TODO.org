#+TITLE: TODO — Planejamento Ativo e Backlog (MyEmacs)
#+AUTHOR: Carlos Filho
#+DATE: 2026-08-05
#+LAST_MODIFIED: 2026-08-22
#+FILETAGS: :PLANNING:
#+OPTIONS: toc:2 num:t
#+STARTUP: content
#+TODO: TODO(t) | DONE(d) CANCELLED(c) BLOCKED(b)

* Visão Geral

Artefato único de planejamento (AGENTS.md). Apenas pendências ativas.
Histórico consolidado no [[file:roadmap.org][roadmap.org]].

- *North Star:* Magent/gptel como driver do Emacs com resiliência e FinOps $0.00.
- *Etapas:* A (Fundação) ✅ → B (Motor) ✅ → C (Ambição) ✅.
- *Últimos marcos:* Resiliência FSM (3 Pilares) ✅, Limpeza & Benchmark CPU aa102-006l ✅, Integ. RFC Mode ✅, Factory Reset 12 Cores ✅.
- *Suíte:* Testes ERT em `tests/` com compilação zero-warning (`compile-prod`).

* Contexto de Arquitetura Oficial do Magent (Referência RAG)

Síntese da documentação oficial do repositório Magent (=docs/AGENT_JOBS.org=, =docs/AGENT_WORKFLOW.org=, =docs/ONBOARDING.org=):

- *Modelo de Subagentes (AGENT_JOBS.org):* Jobs duráveis com a struct =magent-agent-job= (=queued=, =running=, =waiting=, =completed=, =failed=, =closed=, =cancelled=). O =wait_agent= assina observadores de transição com deadline timer (sem polling). Herança de contexto delimitada por =magent-child-agent-max-depth= (padrão 1 nível).
- *Ledger & State Machine (AGENT_WORKFLOW.org):* Hierarquia Codex =thread -> turn -> item=. Turno gerenciado em =magent-ledger.el=. Itens de ferramenta unificados (=tool-call= e =tool-output= no mesmo registro por =call-id=). Persistência dupla via =Snapshot= (restauração rápida de thread) + =Journal= (log append-only).
- *Camadas de Arquitetura (ONBOARDING.org):* 7 camadas. Camada 2 (Sessão & Ledger), Camada 3 (Agentes e Permissões =allow=/=deny=/=ask=), Camada 4 (15 ferramentas nativas em =magent-tools.el= + aprovações em =magent-approval.el=), Camada 5 (Loop Magent e transport fino =gptel-request= em =magent-llm-gptel.el=), Camada 6 (Frontend =agent-shell= + protocolo ACP em =magent-acp.el=).

* Pendente

** Benchmarking Empírico e Matriz Local 14B (Soberania Offline)
    :PROPERTIES:
    :CREATED: 2026-08-22
    :ASSIGNEE: coder
    :STATUS: pendente
    :END:

*** Varredura de Provedores e Modelos MLX/Ollama
Efetuar varredura completa dos modelos instalados no Mac M2 (=agnes=) através de múltiplos provedores (Ollama, MLX, mlx-community e servidor =mlx_lm.server=).
- Identificar e remover modelos pesados/obsoletos acumulados de testes anteriores.

*** Benchmarking Empírico e Matriz Local 14B (Soberania Offline)
Executar benchmarks reais medindo TTFT, tokens/segundo e consumo de memória unificada para a suíte local de 14B:
- Validar =mlx-community/Qwen3.5-Coder-14B-Instruct-4bit= para o perfil =coder=.
- Validar =mlx-community/DeepSeek-R1-Distill-Qwen-14B-4bit= para o perfil =planner=/=sec-ops=.
- Validar =mlx-community/gemma-4-e2b-it-4bit= ou =Qwen-7B= como Orquestrador Local offline.

** Ponte Proxy HTTP Local OpenAI API (AGY CLI Bridge)
    :PROPERTIES:
    :CREATED: 2026-08-22
    :ASSIGNEE: coder
    :STATUS: pendente
    :END:

*** Micro-serviço Proxy Python standalone (bin/agy-proxy)
Desenvolver o servidor HTTP assíncrono em Python (stdlib asyncio/http.server) escutando na porta 8088.
- Implementar o endpoint REST POST /v1/chat/completions com suporte ao formato OpenAI JSON.
- Extrair o histórico de mensagens e parâmetros do payload.
- Mapear os modelos agy-flash e agy-pro para invocação do binário agy CLI.
- Converter a saída stdout em streaming SSE no formato data: {"choices": [...]} terminando com [DONE].

*** Backend gptel e Salvaguarda Auto-Start (lisp/custom-ai.el)
Registrar o backend AGY Proxy no gptel e garantir resiliência de execução.
- Criar a inicialização do backend gptel-make-openai para AGY Proxy na porta 8088 com modelos agy-flash e agy-pro.
- Implementar a função de verificação de porta +carlos/ensure-agy-proxy-running.
- Iniciar o subprocesso bin/agy-proxy automaticamente no Emacs caso a porta 8088 esteja fechada.
- Integrar o AGY Proxy na cascata do roteador dinâmico +carlos/gptel-dynamic-router-advice.

*** Integração com Orquestrador e Matriz de Subagentes Magent
Atualizar o roteamento de modelos no Magent para utilizar a ponte AGY com Custo Zero ($0.00).
- Atualizar +carlos/magent-subagent-profiles em lisp/custom-magent-subagent.el direcionando perfis especialista (coder, planner, sysadmin, auditor, sec-ops, qa) para o backend AGY Proxy.
- Atualizar +carlos/magent-host-profiles em lisp/custom-magent-fsm.el adicionando o AGY Proxy como backend soberano para os hosts aa102-006l e agnes.
- Testar a comunicação FSM e invocação de subagentes via ponte agy.

* Concluído (referências ao roadmap)

- O histórico completo de todas as tarefas concluídas (Suíte DevOps, NixOS, Submódulos, Rust, Polyglot, Forge, Introspecção RAG, Resiliência FSM, etc.) é mantido estritamente em [[file:roadmap.org][roadmap.org]] como Linha do Tempo oficial.

