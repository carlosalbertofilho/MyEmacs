#+TITLE: TODO — Planejamento Ativo e Backlog (MyEmacs)
#+AUTHOR: Carlos Filho
#+DATE: 2026-08-05
#+LAST_MODIFIED: 2026-08-22
#+FILETAGS: :PLANNING:
#+OPTIONS: toc:2 num:t
#+STARTUP: content
#+TODO: TODO(t) | DONE(d) CANCELLED(c) BLOCKED(b)

* Visão Geral

Artefato único de planejamento (AGENTS.md). Apenas pendências ativas.
Histórico consolidado no [[file:roadmap.org][roadmap.org]].

- *North Star:* Magent/gptel como driver do Emacs com resiliência e FinOps $0.00.
- *Etapas:* A (Fundação) ✅ → B (Motor) ✅ → C (Ambição) ✅.
- *Últimos marcos:* Resiliência FSM (3 Pilares) ✅, Limpeza & Benchmark CPU aa102-006l ✅, Integ. RFC Mode ✅, Factory Reset 12 Cores ✅.
- *Suíte:* Testes ERT em `tests/` com compilação zero-warning (`compile-prod`).

* Contexto de Arquitetura Oficial do Magent (Referência RAG)

Síntese da documentação oficial do repositório Magent (=docs/AGENT_JOBS.org=, =docs/AGENT_WORKFLOW.org=, =docs/ONBOARDING.org=):

- *Modelo de Subagentes (AGENT_JOBS.org):* Jobs duráveis com a struct =magent-agent-job= (=queued=, =running=, =waiting=, =completed=, =failed=, =closed=, =cancelled=). O =wait_agent= assina observadores de transição com deadline timer (sem polling). Herança de contexto delimitada por =magent-child-agent-max-depth= (padrão 1 nível).
- *Ledger & State Machine (AGENT_WORKFLOW.org):* Hierarquia Codex =thread -> turn -> item=. Turno gerenciado em =magent-ledger.el=. Itens de ferramenta unificados (=tool-call= e =tool-output= no mesmo registro por =call-id=). Persistência dupla via =Snapshot= (restauração rápida de thread) + =Journal= (log append-only).
- *Camadas de Arquitetura (ONBOARDING.org):* 7 camadas. Camada 2 (Sessão & Ledger), Camada 3 (Agentes e Permissões =allow=/=deny=/=ask=), Camada 4 (15 ferramentas nativas em =magent-tools.el= + aprovações em =magent-approval.el=), Camada 5 (Loop Magent e transport fino =gptel-request= em =magent-llm-gptel.el=), Camada 6 (Frontend =agent-shell= + protocolo ACP em =magent-acp.el=).

* Pendente

** Benchmarking Empírico e Matriz Local 14B (Soberania Offline)
    :PROPERTIES:
    :CREATED: 2026-08-22
    :ASSIGNEE: coder
    :STATUS: pendente
    :END:

*** Varredura de Provedores e Modelos MLX/Ollama
Efetuar varredura completa dos modelos instalados no Mac M2 (=agnes=) através de múltiplos provedores (Ollama, MLX, mlx-community e servidor =mlx_lm.server=).
- Identificar e remover modelos pesados/obsoletos acumulados de testes anteriores.

*** Benchmarking Empírico e Matriz Local 14B (Soberania Offline)
Executar benchmarks reais medindo TTFT, tokens/segundo e consumo de memória unificada para a suíte local de 14B:
- Validar =mlx-community/Qwen3.5-Coder-14B-Instruct-4bit= para o perfil =coder=.
- Validar =mlx-community/DeepSeek-R1-Distill-Qwen-14B-4bit= para o perfil =planner=/=sec-ops=.
- Validar =mlx-community/gemma-4-e2b-it-4bit= ou =Qwen-7B= como Orquestrador Local offline.

** Ponte Proxy HTTP Local OpenAI API (AGY CLI Bridge & 3-Tier Fallback)
    :PROPERTIES:
    :CREATED: 2026-08-22
    :ASSIGNEE: coder
    :STATUS: pendente
    :END:

*** Micro-serviço Proxy Python standalone (bin/agy-proxy)
Desenvolver o servidor HTTP assíncrono em Python (stdlib asyncio/http.server) escutando na porta 8088.
- Implementar o endpoint REST POST /v1/chat/completions com suporte ao formato OpenAI JSON.
- Extrair o histórico de mensagens e parâmetros do payload.
- Mapear os modelos agy-flash e agy-pro para invocação do binário agy CLI (habilitando Google Stitch e capacidades visuais).
- Implementar failover transparente: ao detectar cota excedida (Rate Limit 429) no agy, comutar automaticamente para a API Gemini (Google AI Studio API) usando a chave de ambiente.
- Converter a saída stdout em streaming SSE no formato data: {"choices": [...]} terminando com [DONE].

*** Backend gptel e Cascata de Redundância (lisp/custom-ai.el)
Registrar o backend AGY Proxy no gptel e integrar na cascata de resiliência em 3 níveis (AGY Proxy -> API Cloud -> Local MLX/Ollama).
- Criar a inicialização do backend gptel-make-openai para AGY Proxy na porta 8088 com modelos agy-flash e agy-pro.
- Implementar a função de verificação de porta +carlos/ensure-agy-proxy-running com auto-start do bin/agy-proxy.
- Integrar a cascata de resiliência no +carlos/gptel-dynamic-router-advice: Nível 1 (AGY Proxy) -> Nível 2 (API Cloud Direct) -> Nível 3 (Agentes Locais MLX no Mac M2 / Ollama no Linux CPU).

*** Integração com Orquestrador e Matriz de Subagentes Magent
Atualizar o roteamento de modelos no Magent para utilizar a ponte AGY com Custo Zero ($0.00) e suporte a Stitch.
- Atualizar +carlos/magent-subagent-profiles em lisp/custom-magent-subagent.el direcionando perfis especialista (coder, planner, sysadmin, auditor, sec-ops, qa) para o backend AGY Proxy.
- Atualizar +carlos/magent-host-profiles em lisp/custom-magent-fsm.el adicionando o AGY Proxy como backend soberano para os hosts aa102-006l e agnes, configurando os modelos locais MLX como fallback no Mac M2.
- Testar a comunicação FSM, invocação de subagentes e streaming via ponte agy.

** Monitoramento Proativo de Cotas, Comando /quota e Failover Pre-emptivo (lisp/custom-magent-quota.el)
    :PROPERTIES:
    :CREATED: 2026-08-22
    :ASSIGNEE: coder
    :STATUS: pendente
    :END:

*** Estruturas de Dados e Tabela de Estado de Cotas (lisp/custom-magent-quota.el)
Desenvolver a estrutura central de cotas e a tabela de estado global para rastreamento continuo dos provedores de IA.
- Definir a struct =magent-quota-entry= com os campos: =provider-id=, =model-id=, =metric-type= (=rate_limit=, =balance=, =budget=, =unlimited=), =percent-remaining=, =used-value=, =limit-value=, =status= (=ok=, =warning=, =critical=, =exhausted=, =offline=), =reset-time-iso= e =last-updated-iso=.
- Criar a hash-table global =+carlos/magent-quota-table= para armazenamento centralizado do estado das cotas dos provedores.
- Implementar funcoes de consulta e predicados de saude (=+carlos/magent-quota-get=, =+carlos/magent-quota-healthy-p= com limiar padrao de 5%).

*** Coletores Assincronos por Provedor (AGY Proxy, Gemini, OpenCode, DeepSeek, Anthropic, MLX/Ollama)
Implementar a infraestrutura de varredura assincrona nao-bloqueante para atualizacao continua do estado de cotas dos provedores.
- Implementar =+carlos/magent-quota-fetch-all= e dispatchers por provedor utilizando requisicoes assincronas (=url-retrieve= / =make-process=).
- Provedor AGY Proxy (Local): coletar contadores de requests/dia via inspecao dos headers de resposta HTTP de =/v1/chat/completions= com reset diario a meia-noite UTC.
- Provedor Google AI Studio API: inspecionar headers HTTP =x-ratelimit-remaining-requests= e =x-ratelimit-remaining-tokens= em chamadas Cloud REST.
- Provedor OpenCode Zen: coletar saldo e budget via scraping JSON SSR do endpoint de faturamento =https://opencode.ai/workspace/*/billing=.
- Provedor DeepSeek API: efetuar consulta REST autenticada ao endpoint =https://api.deepseek.com/user/balance= retornando saldo prepaid em USD.
- Provedor Anthropic: calcular limites deslizantes de 5h e 7d a partir do status local do CLI =claude-auth-status-json= ou headers REST.
- Provedor Local MLX / Ollama: efetuar probe via HTTP GET para =http://localhost:11434/api/tags= e verificar integridade de memoria/processos locais.

*** Interface do Usuario, Comando Slash /quota e Menu Transient (C-c A m)
Criar a camada de apresentacao para inspecao de cotas e integracao no ambiente visual.
- Implementar o comando slash =/quota= e a funcao interativa =+carlos/magent-quota-display= para renderizar o buffer temporario =*Magent Quotas*= formatado em tabela Org-mode com codificacao visual por status (OK, WARNING, CRITICAL, EXHAUSTED, OFFLINE).
- Integrar o atalho de exibicao no menu Transient do Magent (=C-c A m= em =lisp/custom-magent-ui.el=).
- Adicionar indicador de cota no mood-line do Emacs (ex: =[AGY 92%]=) refletindo o status do backend primario em tempo real.

*** Mecanismo de Failover Pre-emptivo no Subagente e FSM (custom-magent-subagent.el & custom-magent-fsm.el)
Implementar o desvio automatico de backend antes da execucao de chamadas LLM para evitar falhas HTTP 429.
- Modificar =+carlos/magent-subagent-resolve= em =lisp/custom-magent-subagent.el= para consultar =+carlos/magent-quota-healthy-p= antes de selecionar o backend preferencial do perfil.
- Quando =percent-remaining= <= 5% ou =status= for =critical= ou =exhausted=, desviar preemptivamente para o proximo nivel da cascata (AGY Proxy -> Google AI Studio -> Local MLX/Ollama).
- Atualizar =+carlos/magent-fsm-subagent-jobs= e a maquina de estados em =lisp/custom-magent-fsm.el= para registrar os eventos de failover pre-emptivo no log de auditoria da sessao.

* Concluído (referências ao roadmap)

- O histórico completo de todas as tarefas concluídas (Suíte DevOps, NixOS, Submódulos, Rust, Polyglot, Forge, Introspecção RAG, Resiliência FSM, etc.) é mantido estritamente em [[file:roadmap.org][roadmap.org]] como Linha do Tempo oficial.

