#+TITLE: TODO — Planejamento Ativo e Backlog (MyEmacs)
#+AUTHOR: Carlos Filho
#+DATE: 2026-08-05
#+LAST_MODIFIED: 2026-08-26
#+FILETAGS: :PLANNING:
#+OPTIONS: toc:2 num:t
#+STARTUP: content
#+TODO: TODO(t) | DONE(d) CANCELLED(c) BLOCKED(b)

* Visão Geral

Artefato único de planejamento (AGENTS.md). Apenas pendências ativas.
Histórico consolidado no [[file:roadmap.org][roadmap.org]].

- *North Star:* Magent/gptel como driver do Emacs com resiliência e FinOps $0.00.
- *Etapas:* A (Fundação) ✅ → B (Motor) ✅ → C (Ambição) ✅.
- *Últimos marcos:* Resiliência FSM (3 Pilares) ✅, Limpeza & Benchmark CPU aa102-006l ✅, Integ. RFC Mode ✅, Factory Reset 12 Cores ✅, P3 dry-run guards ✅, Linters nativos + helpers portáteis ✅.
- *Suíte:* Testes ERT em `tests/` com compilação zero-warning (`compile-prod`).

* Plano de Execução — Fases 6 e 4 (2026-08-25)

Ordem de implementação baseada em dependências, risco e alavanca.

** Fase A — Quick Wins (paralelos, independentes)
  - [X] F6-1: =toggle_checkbox= no =org_smart_edit= (Simple/Low) → commit 606c47c
  - [X] F6-6: Integrar =lint-native= no pipeline (Medium/Low) → commit 606c47c

** Fase B — Fundação (sequencial, blocks downstream)
  - [X] F6-2: =+carlos/magent-project-root= utility (Simple/Medium — alto blast radius, ~40 call sites) → commit bc763fd
  - [X] F6-3/F6-5: Evoluir =bin/magent-cli= (Medium/Low — CLI já existe, evolui) → commits c95438b, 5fcc39d
  - [ ] F6-3b: =magent-cli run= — agent launcher batch para Granite e orquestração (Medium/Medium)

** Fase C — Infraestrutura Core (sequencial)
  - [X] F4-select: preferência explícita de backend no =select_model= (Medium/Medium) → commit d23a69c
  - [ ] F4-cb: Circuit Breaker async fix (Medium-High/High — advice chain + FSM loop)
  - [ ] F4-router: validação proxy cross-machine (Simple/Low — agendado 26/08)

** Fase D — Integração (sequencial, alta coordenação)
  - [ ] F4-proxy: AGY Proxy gptel bridge (Medium/Medium)
  - [ ] F4-quota: monitoramento de cotas (Complex/High — módulo novo, 6+ coletores)
  - [ ] F4-toolserver: Tool Server Bridge capstone (Complex/High — depende de tudo)

** Fase E — Avaliação (paralelo)
  - [ ] F6-g: Testar Granite 4.1 locais (Medium/Medium — benchmark hardware)
  - [ ] F7-ben: Bateria de benchmark Code-Repair — 7 modelos MLX/LM Studio locais × 9 profiles × 3 linguagens (Go/React/Python) (Complex/High)

** TODO F7-ben: Bateria de Benchmark Code-Repair — Modelos Locais × Profiles
   :PROPERTIES:
   :CREATED: 2026-08-27
   :STATUS:  EM ANDAMENTO
   :END:

   Avaliar como modelos locais diagnosticam e corrigem código quebrado em
   função do *profile* do agente (system prompt) e da linguagem. Objetivo:
   escolher o melhor modelo local por perfil e dimensionar o que manter no
   roteamento Magent.

   - *Modelos novos a baixar (via =lmstudio= / GGUF, backend =LM Studio Local= em 127.0.0.1:1234):*
     - [ ] =prism-ml/Ternary-Bonsai-27B-mlx-2bit= — Bonsai 27B ternary (ou GGUF via LM Studio)
     - [ ] =nvidia/NVIDIA-Nemotron-3-Nano-4B= (GGUF via LM Studio; sem MLX oficial)
     - [ ] =lmstudio-community/granite-4.0-h-tiny-MLX-8bit= — Granite 4.0 H Tiny Q8 (MLX)
     - [ ] =mlx-community/GLM-4.6V-Flash-4bit= — GLM 4.6V Flash 4bit (MLX)
   - *Modelos baseline já disponíveis (backend =MLX Local= em 127.0.0.1:8081):*
     - [X] =mlx-community/Qwen3.5-9B-MLX-4bit=
     - [X] =mlx-community/gemma-4-e2b-it-4bit=
     - [X] =mlx-community/DeepSeek-R1-Distill-Qwen-14B-4bit=
     - [ ] + config: =Qwen3.5-Coder-7B/14B=, =gemma-4-9b=, =Qwen2.5-Coder-7B= (se instalados)
   - *Total: 7 modelos × 9 profiles × 3 linguagens = 189 runs*

   - *Estado do benchmark (executado — resultado: Timeouts)*
     - [X] Run baseline MLX executado (gemma-4-e2b, Qwen3.5-9B, DeepSeek-R1-14B)
     - [X] Resultado: TODOS deram Timeout a 600s (TTFT >2min por contexto massivo)
     - [X] Causa-raiz: contexto de tools+prompt gigante (payload das tools) → modelos locais demoram demais no 1º token de reasoning

   - *Mitigação Diet Tooling (aplicada)*
     - [X] Coder Lite: filtro dinâmico de =magent-enable-tools= → 14 tools vitais quando backend local (MLX/Ollama/LM Studio) — commit =f893090=
     - [X] Diet Tooling nos schemas: remoção de =:description= dos =:args= de todas as tools (8 arquivos) — commit =6ac69ff=
     - [ ] Smoke test: confirmar que turno de modelo local conclui em batch (=magent-cli run=) pós-fix

   - *Linguagens e bugs de código quebrado:*
     - [ ] Go (=go test= para validar): broker.go com data race, goroutine leak, nil deref
     - [ ] React/TS (=npm test=/eslint): componente com useEffect infinite loop, stale closure, key warning
     - [ ] Python (=pytest=): função O(n²)→O(n), resource leak, wrong exception type

   - *Profiles × ferramentas-chave (de =+carlos/magent-expert-team= em =custom-magent-team.el=):*
     - [ ] =coder=: read, buffer, flycheck_errors, lsp_navigation, 10× smart-edit, grep, glob, forge_read_issue, magit merge/rebase
     - [ ] =qa=: flycheck_errors, emacs_eval, todas smart-edit, spawn_agent/wait_agent, magit diff/log
     - [ ] =auditor=: read, flycheck_errors, lsp_navigation, magit diff/log, rfc, smart-edit c/go/rust/org/sh/markdown
     - [ ] =explore=: read, grep, glob, buffer
     - [ ] =sysadmin=: read/write/edit/bash, todos magit, todos forge, nix/sh smart-edit, docker/systemd, spawn_agent
     - [ ] =planner=: read/write/edit/buffer, org/markdown smart-edit
     - [ ] =tech-writer=: rag_create_doc, smart-edit docs, forge
     - [ ] =sec-ops=: read/bash, sh smart-edit, rfc, docker/systemd
     - [ ] =general=: permissões padrão

   - *Como testar cada combinação (via =magent-cli run=):*
     - [ ] Para cada (modelo, profile, linguagem): copiar =broken-X.ext= para dir temp, rodar
           =magent-cli run --model "LM Studio Local/<model>" --profile <p> --prompt "Corrija este código"=
     - [ ] Validar: rodar o teste da linguagem (=go test= / =npm test= / =pytest=) após a correção
     - [ ] Medir: acurácia (teste passa?), tempo de resposta, número de tool calls, diff gerado vs esperado
     - [ ] Gravar JSONL/JSON resultados em =/tmp/bench-results/=
     - [ ] Script =bin/bench-code-repair.py= para orquestrar a matriz e sumarizar

   - *Saída esperada:* tabela Modelo × Profile de acurácia/latência por linguagem →
      recomendar retenção e atribuição de modelo no roteamento Magent

   - *Bloqueio atual: =Wrong type argument: gptel-tool= no =gptel-request= batch:*
     - [X] Diagnóstico (CAUSA-RAIZ): 5 arquivos custom injetam **plists** cruas em
           =gptel-tools= — =custom-magent-tool-{ast,debug,lsp,pkg,test}.el= — mas o
           gptel moderno exige **structs** =gptel-tool= (via =gptel-make-tool=,
           =gptel-request.el:1597=). Todo request que carrega =gptel-tools= global
           morre com =Wrong type argument: gptel-tool=, independente do backend —
           o que parecia "hang" no =emacs --batch= era esse erro fatal.
           Ferramentas de =custom-magent-tools.el= e as 15 nativas do magent já
           usam =gptel-make-tool= corretamente.
      - [X] Plano de correção (por tool, em cada arquivo):
        - [X] Criar struct =gptel-make-tool= (=:name=, =:function=, =:description=,
              =:args= no formato =(:name X :type Y :description Z)=).
        - [X] Registrar a plist =(:name ... :tool <struct> :permission ...)= em
              =magent-tools-catalog= (roteamento por permissão do Magent).
        - [X] Adicionar a permissão em =magent-enable-tools=.
      - [X] Escopo (5 arquivos): =magent_ert_runner= (test), =magent_treesit_query=
            (ast), =magent_tool_lsp= (lsp), =magent_elpaca= (pkg), =magent_debug=
            (debug).
      - [X] Validação: =check-parens= em cada arquivo + =just compile= zero-warning
            + =just test-all= + repro =magent-cli run= concluindo sem
            =Wrong type argument=.
      - [X] Pós-validado: synchronize prod (=just sync= + =rm -f
            ~/.config/emacs/lisp/*.elc= + =just compile-prod=) e reabrir a
            F7-ben.

** TODO Otimizações Nativas de Boot (early-init) — Apple Silicon + estação NixOS Intel
   :PROPERTIES:
   :CREATED: 2026-08-27
   :STATUS:  EM ANDAMENTO
   :END:
   Cirurgia no =early-init.el= para reduzir o time-to-first-token de boot,
   que hoje concentra peso no carregamento dos =.eln= atrasados. O Emacs age
   por padrão como se rodasse em Pentium 4; em arquiteturas unificadas
   M1/M2 da Apple isso castiga fortemente.
   - [X] Adicionar =native-comp-compiler-options= por host (=system-type=):
         - agnes (darwin/aarch64): =-O2 -mcpu=apple-m1 -mtune=apple-m1=
           (NÃO =apple-m2= — flag mais conservadora, portável no libgccjit
           do nix-darwin/Homebrew)
         - aa102-006l/nanami (gnu/linux/x86_64, Intel Coffee Lake i7-8700):
           =-O2 -march=x86-64-v2= (i7-8700 não suporta plenamente v3; NÃO
           usar =native= — host-bloqueia o .eln)
         - fallback: =-O2= (sweet spot do Emacs, default do bytecode)
   - [X] Alargar o GC threshold no boot: =(* 100 1024 1024)= (100 MiB de
         fato; corrigido bug de aritmética do antigo =(* 50 1000 1000)= que
         era ~47.7 MiB). Alinhado com a FSM do Magent (100 MiB em transições).
   - [X] =native-comp-async-jobs-number= = =(min (num-processors) 8)= —
         dinâmico por contagem real, com teto de 8 (i7-8700 tem 12 threads;
         12 jobs simultâneos engasgam o desktop).
   - [X] Considerar a estação NixOS (=~/Projects/Nixos/MyMachine=,
         =hosts/elitedesk/configuration.nix=): Intel i7-8700, não aplicar
         apple-m2 cegamente — flags por =system-type=.
   - Commit: =9a12d69= (perf boot). Validado: PARENS OK + =just check= boot
     "Config loaded OK".
   - [ ] Pendências de validação: confirmar em prod (=just sync= + =just
         rebuild-prod=) que o boot acelera e que =just compile-prod= escala
         em 8 cores sem warning.
    - [ ] REGRESSÃO DETECTADA (outra sessão): 2 testes =magent-*=
          (=myemacs-magent-subagent-override-consumed= e
          =myemacs-magent-subagent-apply-profile-unknown-agent=) falham com
          =wrong-type-argument gptel-backend= — introduzida pelo commit
          =f893090= (coder-lite) que chama =gptel-backend-name= incondicional
          em =+carlos/magent-subagent-apply-profile= quando o backend é plist
          =(:name ...)= ou string =local= (não struct =gptel-backend=).
          NÃO relacionada ao early-init; precisa correção no
          =custom-magent-subagent.el= (guardar com =gptel-backend-p=).
    - **Progresso da correção (sessão 2026-08-27/28, commit a aplicar):**
      - Criei o helper funções-total =+carlos/--magent-backend-name= e
        substituí a linha do backend em =+carlos/magent-subagent-apply-profile=
        para usá-lo. Passei a usar a ferramenta nativa =replace_text=
        (recém-adicionada ao =elisp-smart-edit= em
        =custom-magent-tool-smart-edit.el=, multi-token =old|new|FLAGS=)
        para aplicar a edição de forma transacional.
      - *Diagnóstico feito:* em probe isolado (com =init.el=), o helper
        retorna corretamente =t= para backend plist =(:name "OpenCode Zen")=,
        símbolo =local= e string =Ollama/y=. O =gptel-backend-p= retorna
        =nil= para plist (correto). O diff do =custom-magent-subagent.el=
        e o bytecode do ==.elc= (via =strings=) confirmam a chamada ao
        helper na linha 184. Removi o =custom-magent-subagent.elc= e rodei
        =just clean= (limpa =.elc=/=.eln=/=eln-cache=) — o teste AINDA falha.
      - *Ponto crítico da tese:* o backtrace do ERT persiste mostrando a
        expansão inline antiga de =gptel-backend-name= (frames
        =(&#x2e83;let* ((cl-x backend-obj))&#x2026;)= e =(&#x2e83;or (let*
        ((cl-x cl-x))&#x2026;)=) e a condição
        =wrong-type-argument gptel-backend (:name "OpenCode Zen")= /
        =(gptel-backend local)= — mesmo sem =elc= e sem =eln-cache=. Isso
        sugere que o código em execução no teste ainda é o antigo.
      - **TESES ATIVAS (por ordem de probabilidade):**
        1. Existe uma SEGUNDA definição de =+carlos/magent-subagent-apply-profile=
           (ou um =advice= / re-definição em outro módulo =custom-magent-*.el=
           carregado depois) que ainda contém o =gptel-backend-name= inline
           antigo. O =load-tests.el= carrega toda a suíte; algum outro arquivo
           pode redefinir a função. -> investigar definições duplicadas de
           =apply-profile= / busca por =gptel-backend-name back-end-obj= em
           TODO o load-path, não só no subagent.el.
        2. O =load-tests.el= carrega o módulo de um caminho distinto (ex.:
           =.elc= em outro diretório do =load-path=, prod =~/.config/emacs=,
           ou pacote =magent= externo) cuja versão é a antiga. O teste roda
           com =--init-directory "$(pwd)"= mas o =load-tests.el= pode
           =require= de outra árvore.
        3. (menos provável) Versionamento por host/condicional que gera
           código diferente no ramo dos testes (ex.: =+carlos/--magent-backend-name=
           definida mas um caminho alternativo não-editado).
      - **Ação a tomar:** procurar TODAS as definições de
        =+carlos/magent-subagent-apply-profile= e todas as referências a
        =gptel-backend-name= com o símbolo =backend-obj= em todo o repo
        (incluindo =tests/= e =site-lisp/=) e verificar qual é carregada
        pelo ERT. Confirmar vazio de =defalias= concorrente =M-x
        find-function-noselect= no batch.
     - **Problema novo no PROD (após =just factory-reset=, 2026-08-27 à noite):**
       - Erro de boot/uso: =envrc--direnv-export: Invalid function:
         envrc--with-required-current-en= ao entrar em diretórios com
         =direnv= (ex.: =~/Projects/42rio/CommonCore/Rank05/Exam/Level0/=).
         Provável problema de =direnv.el= / =envrc= (versão ou hook nativo-
         compilado stale) no ambiente oficial =~/.config/emacs=.

** TODO Diagnóstico e Correção — Fechamento Prematuro da GUI no Modo Daemon
   :PROPERTIES:
   :CREATED: 2026-08-28
   :STATUS:  EM ANDAMENTO
   :END:

   No modo daemon (=emacs --daemon= ou serviço systemd), ao abrir cliente gráfico
   com =emacsclient -c=, a janela GUI é iniciada e fecha imediatamente.

   - *Diagnóstico do Log e Causa-Raiz:*
     - *Limitação PGTK/GTK3 (Wayland):* Build do Emacs com =--with-pgtk= possui
       limitação inerente no GTK3 onde desconexões de display ou inicialização
       incompleta de frame resultam no encerramento imediato do processo daemon
       (=Emacs built with PGTK will simply exit when a display connection is closed=).
     - *Hook de Frame não protegido em custom-dashboard.el:* A função
       =+carlos/dashboard-open-on-server-frame= é invocada em
       =server-after-make-frame-hook= chamando =(dashboard-open)= diretamente sem
       guarda de exceção (=with-demoted-errors= / =ignore-errors=).
     - *Variáveis de ambiente de GUI no Systemd:* Daemons iniciados via
       =systemd --user= podem ser criados antes da sessão gráfica, sem
       =WAYLAND_DISPLAY= / =DISPLAY= / =XDG_RUNTIME_DIR=.

   - *Plano de Correção:*
     - [ ] Envolver =+carlos/dashboard-open-on-server-frame= em =with-demoted-errors= em =custom-dashboard.el=.
     - [ ] Adicionar guarda =display-graphic-p= e checagem de frame de servidor antes de renderizar dashboard.
     - [ ] Documentar comando =systemctl --user import-environment DISPLAY WAYLAND_DISPLAY XDG_RUNTIME_DIR= para inicialização via systemd.

      - Ao abrir arquivo =.py= carrega em modo =Fundamental= (Python
        mode/treesit não ativando) no prod. Suspeita de =eglot=/
        =python-mode= ou =treesit= quebrado após o factory-reset
        (gramáticas ou autoloads não instalados).
      - Ambos são do AMBIENTE PROD (pós factory-reset), independentes da
        regressão do repo. Registrar e investigar à parte (possível
        =just rebuild-prod= + =just install= para reinstalar dependências
        nativas/gramáticas).
   Proto-plano validado pelo Gemini web; estabilizar os Timeouts do CLI
   (=magent-cli run= batch) antes de mexer nos ponteiros do early-init.


* Contexto de Arquitetura Oficial do Magent (Referência RAG)

Síntese da documentação oficial do repositório Magent (=docs/AGENT_JOBS.org=, =docs/AGENT_WORKFLOW.org=, =docs/ONBOARDING.org=):

- *Modelo de Subagentes (AGENT_JOBS.org):* Jobs duráveis com a struct =magent-agent-job= (=queued=, =running=, =waiting=, =completed=, =failed=, =closed=, =cancelled=). O =wait_agent= assina observadores de transição com deadline timer (sem polling). Herança de contexto delimitada por =magent-child-agent-max-depth= (padrão 1 nível).
- *Ledger & State Machine (AGENT_WORKFLOW.org):* Hierarquia Codex =thread -> turn -> item=. Turno gerenciado em =magent-ledger.el=. Itens de ferramenta unificados (=tool-call= e =tool-output= no mesmo registro por =call-id=). Persistência dupla via =Snapshot= (restauração rápida de thread) + =Journal= (log append-only).
- *Camadas de Arquitetura (ONBOARDING.org):* 7 camadas. Camada 2 (Sessão & Ledger), Camada 3 (Agentes e Permissões =allow=/=deny=/=ask=), Camada 4 (15 ferramentas nativas em =magent-tools.el= + aprovações em =magent-approval.el=), Camada 5 (Loop Magent e transport fino =gptel-request= em =magent-llm-gptel.el=), Camada 6 (Frontend =agent-shell= + protocolo ACP em =magent-acp.el=).

* Pendente

** TODO Integração de JSON Schema e GBNF no Emacs (MLX/Ollama)
   :PROPERTIES:
   :STATUS: BACKLOG
   :END:
   A utilização de Grammar (via GBNF no llama.cpp/Ollama) ou JSON Schema (no MLX) no Emacs é perfeitamente viável e já está integrada às ferramentas mais modernas do ecossistema.
   - =gptel=: Usar o argumento =:schema= na API de requisições (=gptel-request=) para forçar a LLM a responder seguindo um JSON Schema.
   - =ellama= / =llm.el=: Suporta =:response-format= usando a especificação do JSON Schema.
   - =GBNF Puro=: Extender requisição HTTP (url-retrieve/request.el) para o endpoint =/completion= injetando um payload com a gramática.

** DONE F6-3b: =magent-cli run= — Agent Launcher Batch
   :PROPERTIES:
   :CREATED: 2026-08-26
   :STATUS:   DONE
   :END:
   Subcomando =run= no =bin/magent-cli= para spawar agentes Magent em batch.
   Útil para testar Granite local e orquestração externa.
   - [X] Decisões: JSON final + JSONL status file, tools do profile, sem benchmark
   - [X] Fix: =INIT_DIR= default de prod para =$(pwd)=
   - [X] Criar =lisp/custom-magent-run.el= com =+carlos/magent-run-execute= e =+carlos/magent-jsonl-sink=
   - [X] Adicionar =require= em =init.el=
   - [X] Adicionar subcomando =run= em =bin/magent-cli= (=--model=, =--profile=, =--prompt=, =--status-file=)
   - [X] Testes ERT em =tests/magent-run-test.el=
   - [X] Atualizar =tests/cli-test.el=
   - [X] Gate: =just compile= zero-warning + =just test-all= zero-failure
   - [X] Fix dispatch: =list-tools= e =describe= como comandos posicionais (commit =1d92db3=)
   Referências: =magent-runtime-submit= (UI-neutral), benchmark runner pattern, lifecycle event sinks.

* Fase 5: Onisciência do Agente e RAG Sintático

* Fase 6: Melhorias UX Batch para Agentes

** DONE 1. Implementar `toggle_checkbox` no `org_smart_edit`
    :PROPERTIES:
    :CREATED: 2026-08-23
    :STATUS:   DONE
    :END:
- [X] Adicionar `toggle_checkbox` à função de orquestração do `org_smart_edit`.
- [X] O comando deve receber o texto/heading alvo e usar `org-toggle-checkbox` nativo do Org.
- [X] Escrever função auxiliar `+carlos/--org-toggle-checkbox-at-target`.
- [X] Adicionar teste na suíte de testes.
Commit: 606c47c. 3 ERTs em tests/smart-edit-test.el.

** DONE 2. Corrigir Resolução de Contexto do Projeto no Batch Mode
    :PROPERTIES:
    :CREATED: 2026-08-23
    :STATUS:   DONE
    :END:
- [X] Criar função utilitária `+carlos/magent-project-root`.
- [X] Lógica com fallback: `(or (project-root ...) (vc-root-dir) default-directory)`.
- [X] Substituir chamadas diretas de `project-root` no core Magent (31 call sites em 4 arquivos).
Commit: bc763fd. 5 ERTs em tests/infra-test.el.

** TODO 3. Criar Wrapper CLI `bin/magent-cli`
    :PROPERTIES:
    :CREATED: 2026-08-23
    :STATUS: BACKLOG
    :END:
- [ ] Criar script executável `bin/magent-cli` (`set -euo pipefail`).
- [ ] Injetar flags `--init-directory "$EMACSDIR" --batch -l init.el` transparentemente.
- [ ] Receber comandos/argumentos Lisp na CLI e avaliar.
- [ ] Ajustar documentação em `docs/magent-reference.org`.

** DONE 6. Integrar linters nativos no pipeline de validação
    :PROPERTIES:
    :CREATED: 2026-08-25
    :STATUS:   DONE
    :END:
- [X] Adicionar `lint-native` ao recipe `lint` (Justfile: `lint: compile checkdoc org-lint lint-native`).
- [X] Adicionar `lint-native` ao recipe `test-all` para garantir que todo commit passe pelos linters nativos.
- [ ] Integrar `+carlos/magent-lint-defun-parens` como gate extra no `elisp-smart-edit` (kind `'code`): rodar antes de `check-parens` para pegar off-by-one mais cedo.
- [X] Adicionar teste ERT que valida que `lint-native` passa no repo inteiro (=myemacs-lint-native-passes=).
- [ ] Atualizar `docs/dev-workflow.org` com a nova pipeline de validação.
Commits: 606c47c (integrado), c759c1a (linters criados). 5 ERTs em tests/lint-test.el.

** DONE 4. Melhorias =org_smart_edit= — ações AST-aware seguras
    :PROPERTIES:
    :CREATED: 2026-08-24
    :STATUS:   DONE
    :END:
Arquivo-alvo: =lisp/custom-magent-tool-smart-edit.el=. Incidente (2026-08-24):
=refactor_symbol= usa =(search-forward old nil t)= sem =regexp-quote= — ao passar
uma string contendo asteriscos de heading (ex.: ="* TODO ** TODO"=), o padrão casou
com todos os headings do arquivo e destruiu o TODO.org inteiro.
    - [X] Helper transacional =+carlos/magent--smart-edit-transaction=: snapshot =(buffer-substring)= antes da mutação, gate pós-mutação (=org-lint= para .org / =check-parens= para código) e restore byte-a-byte em erro. Substitui o =primitive-undo= mal usado nas variantes elisp/go (espera undo-list, não buffer — rollback atual é fictício).
    - [X] Endurecer =refactor_symbol=: flags opcionais =FIRST=|=WORD=|=REGEX=|=ALL= (default =ALL=, backward-compat), executando dentro do helper transacional. Diagnóstico corrigido: =search-forward= já é literal — o perigo real é substituição global sem âncora AST e sem transação (incidente 2026-08-24).
    - [X] Nova ação =replace_heading= (AST-aware): casar =:raw-value= exato via =org-element-map=, editar apenas a linha do heading preservando STARS/KEYWORD/tags (~20 linhas). Prioridade Alta.
    - [X] Ancorar =set_property= no mesmo primitivo AST (a busca solta atual pode definir propriedade no nó errado).
    - [X] Nova ação =replace_text= com range validado (args: ="old|new|L1|L2"=; erro se L1>L2 ou fora do buffer; reporta contagem). Prioridade Média.
    - [X] Auditoria de undo concluída (2026-08-24): ver relatório; 17 =primitive-undo= quebrados; markdown sem gate; branch-delete sem guarda; elpaca read-only.
    - [X] P1: wiring transacional das 9 variantes restantes.
    - [X] P2: markdown adotar gate.
    - [X] P3: guardas FORCE/dry-run (branch-delete, docker rm, systemd disable).
      - Implementação: adicionar parâmetro =dry_run= (string "true") em 3 tools destrutivas.
      - Quando =dry_run= é "true": retorna o comando que seria executado SEM executar.
      - Flag =destructive= no resultado quando a ação é destrutiva (delete/rm/disable/stop).
      - Arquivos: =custom-magent-tool-git.el= (=branch-delete=), =custom-magent-tool-devops.el= (=docker-action=, =systemd-action=).
      - Atualizar definições =gptel-make-tool= com o novo parâmetro.
      - Testes ERT para dry-run em cada tool.
    - [X] Testes ERT: regressão do incidente (padrão com asteriscos não vaza além do alvo), rollback forçado (arquivo restaurado idêntico byte-a-byte), contratos das ações novas e das flags.
    - [X] Atualizar =docs/magent-reference.org= (regra de ouro: API Magent documentada) e flip deste nó para DONE.
- Link to plan: [[file:../.gemini/antigravity-cli/brain/6939fb0d-b068-4595-850d-99e9321fe46f/plan_consolidated.md][Plano Consolidado §2]]

** TODO Testar modelos IBM Granite 4.1 locais (Ollama/MLX) — Mac Mini M2
    :PROPERTIES:
    :CREATED: 2026-08-24
    :STATUS: BACKLOG
    :END:
Hardware: Mac Mini M2 / 24 GB — memória unificada CPU-GPU (~15-18 GB úteis para modelos). Ollama v0.32.5 e MLX (porta 8081) já instalados.
Candidatos sweet spot:
    - [ ] Granite 4.1 8B (~5.5 GB): linguagem/RAG; substituto direto do =qwen2.5-coder:3b= com qualidade superior.
    - [ ] Granite Code 4.1 8B: geração de código enterprise; licença Apache 2.0 IP-safe.
    - [ ] Granite Vision 4.1 4B (~3 GB): nova capability — extração de PDFs/tabelas.
    - [ ] Granite Speech 4.1 2B Plus (~1.5 GB): nova capability — transcrição local com diarização.
Critério: pull via Ollama + benchmark de latência/qualidade contra os modelos atuais da matriz de roteamento; avaliar adoção como subagentes Magent (=min-tier= free).
Veredito da análise (2026-08-24):
    - ORQUESTRADOR: nenhum Granite. O acumulador do FSM (=custom-magent-fsm.el=) extrai tool calls de dentro do bloco reasoning =<thought>= do Gemma; Granite não tem reasoning nativo — chamadas emitidas ali seriam perdidas. Manter =gemma4= E2B como orquestrador local (30 tok/s no MLX, turno ~1.40s vs ~15-20 tok/s de um Granite 8B).
    - [ ] Subagente =coder=: testar Granite Code 4.1 8B substituindo Qwen3.5-Coder-7B.
    - [ ] Subagente =tech-writer=: testar Granite 4.1 8B substituindo Gemma-4-9B.
    - [ ] Subagente =explore=/=qa=: testar Granite 4.1 3B substituindo Qwen2.5-Coder.
    - [ ] Capabilities novas a avaliar: Vision 4B (extração de PDFs/tabelas), Guardian 8B (safety), Embedding 278M (RAG local).

** TODO 5. Evoluir =bin/magent-cli= — CLI auto-descritiva
    :PROPERTIES:
    :CREATED: 2026-08-24
    :STATUS: BACKLOG
    :END:
Estado atual: wrapper mínimo (28 linhas) sem =--help=, sem listagem de ferramentas,
sem validação de argumentos, output bruto (JSON inconsistente), sem =--dry-run= e
sem =--init-directory=. Interface alvo: =bin/magent-cli [FLAGS] <tool_name> [args...]=.
- [ ] Flag =--list-tools= / =-l=: listar todas as funções =+carlos/magent-tool-*= com docstring resumida, via =(apropos-internal "\\+carlos/magent-tool-" #'fboundp)= + =(documentation fn)= (~30 min). Prioridade Alta.
- [ ] Flag =--describe <tool>=: docstring + argumentos via =(help-function-arglist fn)= + =(documentation fn)=, com ações e exemplos por convenção de nome (~30 min). Prioridade Média.
- [ ] Flag =--dry-run=: imprimir o Elisp que seria executado sem executar — útil para agentes verificarem antes de commitar (~10 min). Prioridade Baixa.
- [ ] Output JSON estruturado (=status=, =tool=, =action=, =output=, =elapsed_ms=) e validação de argumentos com mensagem de erro apontando =--describe <tool>= (~20 min). Prioridade Média.
- [ ] Flag =--timeout <seconds>= (default: 60, evita travamento indefinido do Emacs batch) e flag =--init-dir <path>= para override do EMACSDIR (default: =~/.config/emacs=). Prioridade Média.
- [ ] Testes ERT das novas capacidades da CLI (~45 min junto com os demais itens do plano). Prioridade Alta.
- Link to plan: [[file:../.gemini/antigravity-cli/brain/6939fb0d-b068-4595-850d-99e9321fe46f/plan_consolidated.md][Plano Consolidado §3]]

* Fase 4: Recursos Avançados (Pós-Refatoração)
    :PROPERTIES:
    :CREATED: 2026-08-22
    :STATUS: BACKLOG
    :END:

** TODO Ponte Proxy HTTP Local OpenAI API (AGY CLI Bridge & 3-Tier Fallback)
Servidor Python (=lib/agy_proxy/=), lifecycle elisp e FSM de cota já operam. Integração gptel restante:
    - [ ] Registrar backend =gptel-make-openai= AGY Proxy (:8088, modelos =agy-flash=/=agy-pro=) em =custom-ai.el=.
    - [ ] Criar =+carlos/ensure-agy-proxy-running= com auto-start reusando o lifecycle de =custom-magent-proxy.el=.
    - [ ] Plugear a cascata de resiliência em 3 níveis no =+carlos/gptel-dynamic-router-advice= (Nível 1 AGY Proxy).

** TODO Monitoramento Proativo de Cotas, Comando /quota e Failover Pre-emptivo (lisp/custom-magent-quota.el)
Estruturas de Dados e Tabela de Estado de Cotas:
- Definir a struct =magent-quota-entry= com os campos correspondentes.
- Criar a hash-table global =+carlos/magent-quota-table=.
- Implementar funcoes de consulta e predicados de saude (=+carlos/magent-quota-healthy-p= com limiar padrao de 5%).

Coletores Assincronos por Provedor:
- Implementar =+carlos/magent-quota-fetch-all= e dispatchers por provedor utilizando requisicoes assincronas (=url-retrieve= / =make-process=).
- Coletar dados de AGY Proxy, Google AI Studio, OpenCode Zen, DeepSeek, Anthropic e Local MLX / Ollama.

Interface do Usuario, Comando Slash /quota e Menu Transient (C-c A m):
- Implementar o comando slash =/quota= para renderizar o buffer temporario =*Magent Quotas*= formatado em tabela Org-mode.
- Integrar o atalho no menu Transient do Magent.
- Adicionar indicador de cota no mood-line do Emacs.

Mecanismo de Failover Pre-emptivo:
- Modificar =+carlos/magent-subagent-resolve= para consultar a saude da cota antes de acionar o LLM.
- Quando a cota for critica, desviar preemptivamente para o proximo nivel da cascata.
- Registrar os eventos de failover pre-emptivo na maquina de estados.

** TODO Fase 7: Structured Outputs e Eliminação de Alucinações
- [ ] 1. **Gerador Dinâmico de Schema no Elisp:**
  - [ ] O Magent precisará compilar as configurações das ferramentas ativas e gerar um schema no formato `{"type": "json_schema", "json_schema": {"name": "magent_tool_call", "strict": true, "schema": {...}}}`.
  - [ ] O Schema deve obrigar o modelo a preencher o campo `"rationale"` (explicando o motivo) ANTES de preencher o `"tool_name"` e os `"args"`. Isso forçará um *Chain of Thought* (CoT) embutido no JSON que melhora o acerto de modelos fracos.
- [ ] 2. **Interceptador (Advice) no Backend HTTP do Gptel:**
  - [ ] Adicionar um advice na montagem do payload HTTP do gptel (como em `gptel--openai-parse-request`).
  - [ ] Se o modo `magent-fsm-mode` estiver ativo E o modelo alvo *não* for de raciocínio (não for DeepSeek R1, pois a tag `<think>` colide com a validação estrita JSON), devemos injetar o `response_format` no JSON da requisição (para MLX/OpenAI).
- [ ] 3. **Validação Prática com Modelo Minúsculo:**
  - [ ] Adicionar uma subtarefa para testar a FSM localmente usando um modelo pequeno e rápido no MLX (ex: `Qwen2.5-Coder-1.5B`) para provar matematicamente que a restrição de logits força a conformidade perfeita ao JSON sem o Magent precisar gastar CPU com Regex defensivo.
  - [ ] Executar benchmark no host =aa102-006l= (mantido habilitado para IA local) utilizando modelos locais fracos via Ollama (GBNF) para avaliar na prática se a eliminação de alucinações compensa a lentidão da CPU.

* Concluído (referências ao roadmap)

- O histórico completo de todas as tarefas concluídas (Suíte DevOps, NixOS, Submódulos, Rust, Polyglot, Forge, Introspecção RAG, Resiliência FSM, etc.) é mantido estritamente em [[file:roadmap.org][roadmap.org]] como Linha do Tempo oficial.
- **2026-08-25:** Phase A — toggle_checkbox + lint-native no pipeline → commit 606c47c → [[file:roadmap.org][roadmap.org]] (Histórico).
- **2026-08-25:** Phase B — +carlos/magent-project-root utility, 31 call sites consolidados → commit bc763fd → [[file:roadmap.org][roadmap.org]] (Histórico).
- **2026-08-25:** P3 dry-run guards (branch-delete, docker-action, systemd-action) → [[file:roadmap.org][roadmap.org]] (Histórico).
- **2026-08-25:** Linters nativos (defun-parens + arity) + helpers portáteis (trim, with-temp-file-buffer) → [[file:roadmap.org][roadmap.org]] (Histórico).
- **2026-08-27:** Correção de Hangs Críticos e Timers no magent-cli (--batch) para pipeline F7-ben → [[file:roadmap.org][roadmap.org]] (Histórico).

* TODO Refatoração SOLID - Fase 7 - Structured Outputs
    :PROPERTIES:
    :CREATED: 2026-08-23
    :STATUS: BACKLOG
    :END:
Plano detalhado inline na seção "Fase 7" acima (L131).

* DONE Refatoração SOLID - Fase 5 - Depurador Vivo
    :PROPERTIES:
    :CREATED: 2026-08-23
    :STATUS: DONE
    :END:

* TODO Refatoração SOLID - Fase 6 - UX Batch
    :PROPERTIES:
    :CREATED: 2026-08-23
    :STATUS: EM ANDAMENTO
    :END:

* TODO Refatoração SOLID - Fase 4 - Ponte Proxy HTTP e Monitoramento

** TODO Componente 5: Servidor de Ferramentas (Tool Server Bridge)
A inteligência da CLI =agy= externa precisa consumir as ferramentas Lisp nativas do Emacs.
O Proxy resolve isso agindo como ponte de inversão de controle.

*** Fluxo FSM Tool Interceptor
1. *Discovery*: O proxy expõe as ferramentas nativas (magit, org_smart_edit, elisp_smart_edit, dape) como array de =tools= na API OpenAI-compatible enviada ao Upstream.
2. *Interceptação*: Quando a API retorna =tool_calls=, o FSM intercepta.
3. *Execução Bridge*: O FSM usa =bin/magent-cli= (rodando =emacs --batch -l init.el=) para executar a função de forma idêntica à versão nativa no Emacs.
4. *Resolução*: O FSM do Proxy retorna os =tool_results= ao LLM transparente para o cliente =agy=.
    :PROPERTIES:
    :CREATED: 2026-08-23
    :STATUS: pendente
    :END:

** TODO Matriz de Roteamento Híbrido e Model Mapping no Proxy
    :PROPERTIES:
    :CREATED: 2026-08-24
    :STATUS: BACKLOG
    :END:
Implementação concluída em =lib/agy_proxy/= (=router.py=, =upstream.py=, =config.py= com =model_mapping=). Restante:
    - [ ] Testar rotas nas duas máquinas: agnes (validado localmente) e aa102-006l — agendado para quarta 2026-08-26.

** TODO Refatorar Circuit Breaker para lidar com falhas assíncronas (gptel-request)
:PROPERTIES:
:ID: opus-cb-async-fix
:HOST: todos
:TYPE: refactor
:STATUS: backlog
:CREATED: 2026-08-24
:END:
- [X] Analisar falhas assíncronas do gptel-request que não acionam o Circuit Breaker.
  Conclusão (logs 2026-08-24 10:12–10:25): erros de billing/quota chegam como
  CONTEÚDO da mensagem assistant (journal =item-failed=/=turn-failed=), não como
  =:error= do gptel — mesmo com a advice funcional seriam classificados como
  sucesso. Além disso nenhum caminho de produção chama =+carlos/magent-cb-execute=
  ou =+carlos/magent-cb-record-failure= (só testes); o CB está desconectado do
  loop de turno da FSM.
- [ ] Implementar advice no callback para registrar falhas, incluindo classificação
  de erro-em-conteúdo (padrões "Insufficient balance", 429, quota/billing) como falha.
- [ ] Conectar o CB ao loop de turno da FSM (registrar falha do backend no
  =turn-failed= e consultar =cb-open-p= antes de cada requisição).
- [ ] Atualizar +carlos/magent-cb-execute para considerar erros HTTP.
- [ ] Testar fallback para modelo Gemini quando OpenCode falhar por quota.
- Link to plan: [[file:../.gemini/antigravity-cli/brain/b9fed757-e3a9-418f-a8d0-e2bd51f17d69/scratch/opus_cb_fix_plan.org][Opus Plan]]

** TODO Roteamento: preferência explícita de backend no select_model + guarda no host-routing
    :PROPERTIES:
    :CREATED: 2026-08-24
    :STATUS: BACKLOG
    :END:
Sintoma: usuário pediu explicitamente Google API/Gemini flash e o Magent manteve
OpenCode Zen (o gptel direto funciona normalmente com Gemini).
- [ ] Adicionar parâmetro =backend-preferred= (e/ou =model=) à tool =select_model=
  (=+carlos/magent-tool-select-model=, =lisp/custom-magent-tools.el:908=) repassado
  como =:preferred-backend= aos hints — hoje a instrução textual do usuário não tem
  caminho até o resolver determinístico (hints vêm só do perfil do agente).
- [ ] Guarda em =+carlos/magent-apply-host-routing= (=lisp/custom-magent-fsm.el:402=):
  quando =gptel-get-backend= não resolver o backend do perfil do host, registrar erro
  visível (warnings gate) em vez de manter silenciosamente o =gptel-backend= residual —
  suspeito de ter enviado os turnos de 10:12–10:25 ao OpenCode Zen em vez do gemma
  local do perfil agnes.
- [ ] Testes ERT dos dois comportamentos.

