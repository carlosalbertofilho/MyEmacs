#+TITLE: TODO — Planejamento Ativo e Backlog (MyEmacs)
#+AUTHOR: Carlos Filho
#+DATE: 2026-08-05
#+LAST_MODIFIED: 2026-09-01
#+FILETAGS: :PLANNING:
#+OPTIONS: toc:2 num:t
#+STARTUP: content
#+TODO: TODO(t) | DONE(d) CANCELLED(c) BLOCKED(b)

* Visão Geral

Artefato único de planejamento (AGENTS.md). Apenas pendências ativas.
Histórico consolidado no [[file:roadmap.org][roadmap.org]].

- *North Star:* Magent/gptel como driver do Emacs com resiliência e FinOps $0.00.
- *Etapas:* A (Fundação) ✅ → B (Motor) ✅ → C (Ambição) ✅.
- *Últimos marcos:* Resiliência FSM (3 Pilares) ✅, Limpeza & Benchmark CPU aa102-006l ✅, Integ. RFC Mode ✅, Factory Reset 12 Cores ✅, P3 dry-run guards ✅, Linters nativos + helpers portáteis ✅, Savepoint v1.1 Hybrid Arch ✅.
- *Suíte:* Testes ERT em =tests/= com compilação zero-warning (=compile-prod=).

* Plano de Execução (Atualizado 2026-09-01)

Ordem de implementação baseada em dependências, risco e alavanca.

** Fase 1 — Estabilização do Ambiente Prod (Imediato)

- [X] Validar correção do erro =envrc--direnv-export: Invalid function= (eliminado loop recursivo infinito de =with-eval-after-load= no =custom-lang.el=; validado via =elisp_eval= e =just check=).

** Fase 2 — Resiliência de Subagentes (Prioridade Alta)

*** TODO Resiliência de Subagentes — Cleanup de Timeout, Watchdog e Resiliência LLM (Magent)
:PROPERTIES:
:CREATED:  2026-09-01
:STATUS:   PLANO APROVADO
:END:

Diagnóstico da cadeia causal: chamadas LLM que penduram no child loop do =spawn_agent= deixam o job em estado =running= indefinidamente quando o =wait_agent= atinge o timeout de 120s.

- *Objetivo:* Eliminar vazamento de jobs em memória/runtime tornando a máquina de estados do Magent totalmente resiliente a timeouts e requisições penduradas.

- *Ações de Correção:*
  - [X] *Correção 1 (wait_agent timeout cleanup):* Em =wait_agent= (=magent-tools.el=), quando =finish= for chamado com status ="timeout"=, abortar child loops não-terminais com =magent-agent-loop-abort=, definir jobs como =cancelled= (erro ="Wait timed out"=) e limpar o runtime (implementado e validado em =custom-magent-subagent.el=).
  - [X] *Correção 2 (Watchdog por job/turno):* Criar timer por job disparado no =spawn_agent= que cancela automaticamente após =magent-request-timeout=, independente de haver observer ativo (implementado com hash-table de timers e observer de cancelamento prévio).
  - [ ] *Correção 3 (Propagação de Timeout do LLM):* Garantir que o =magent-agent-loop= e a camada de transporte gptel falhem e propaguem erro após um teto de tempo, evitando pendurar silenciosamente.
  - [ ] *Validação & ERT:* Criar testes em =tests/= e validar compilação zero-warning (=compile-prod=) e suíte de regressão.

*** TODO Resiliência do Sentinel e Interação com Orquestrador (UX)

- [X] Implementar um =condition-case= global no wrapper de execução de tools (=magent-tool-result-require=): intercepta retornos primitivos ou exceções nativas (como =wrong-type-argument=) e encapsula em =magent-tool-result= com status failed/completed sem derrubar o Sentinel (validado via teste ERT =myemacs-magent-tool-result-require-resilience-test=).
- [ ] Desenvolver Injeção de Contexto (Prompt Side-band) no =shell-maker=:
  - [ ] Aplicar um /advice/ no =shell-maker-submit= para que, quando a FSM estiver no estado =waiting_agent= ou gerando, o prompt não fique travado lançando erro.
  - [ ] A interceptação deve capturar o texto do usuário, salvar no =session.json= e emitir um sinal de *wake-up* ou interrupção suave para o loop da FSM do Orquestrador.
  - [ ] Como fallback, expor um atalho global (=C-c A i=) e uma opção no menu Transient (=C-c A m= -> =[I] Interromper Sessão=) para forçar o destravamento.

*** DONE Blindar Orquestrador contra Alucinação de Ferramentas
:PROPERTIES:
:CREATED: 2026-09-01
:STATUS:  DONE
:END:

- [X] Ocultar (un-register) nativamente ferramentas de mutação (=*_smart_edit=, =write_to_file=, =replace_file=) do contexto do perfil =orchestrator= (implementado em =custom-magent-subagent.el= e testado via ERT).
- [X] Reforçar a validação do parâmetro =prompt= na tool =spawn_agent= para rejeitar strings literais como =[Insert Synthesis Here]= ou strings vazias.

** Fase 3 — Bateria de Benchmark Local (F7-ben)

*** TODO F7-ben: Bateria de Benchmark Code-Repair — Modelos Locais × Profiles
:PROPERTIES:
:CREATED: 2026-08-27
:STATUS:  EM ANDAMENTO
:END:

Avaliar como modelos locais diagnosticam e corrigem código quebrado em
função do *profile* do agente (system prompt) e da linguagem. Objetivo:
escolher o melhor modelo local por perfil e dimensionar o que manter no
roteamento Magent.

- *Modelos novos a baixar (via =lmstudio= / GGUF, backend =LM Studio Local= em 127.0.0.1:1234):*
  - [ ] =prism-ml/Ternary-Bonsai-27B-mlx-2bit= — Bonsai 27B ternary (ou GGUF via LM Studio)
  - [ ] =nvidia/NVIDIA-Nemotron-3-Nano-4B= (GGUF via LM Studio; sem MLX oficial)
  - [ ] =lmstudio-community/granite-4.0-h-tiny-MLX-8bit= — Granite 4.0 H Tiny Q8 (MLX)
  - [ ] =mlx-community/GLM-4.6V-Flash-4bit= — GLM 4.6V Flash 4bit (MLX)
- *Inference Engine Alternativa (Teste Colibre AI)*:
  - [ ] Validar =coli= (Colibre AI) no macOS contra o MLX. Medir Tok/s e TTFT.
- *Total: 7 modelos × 9 profiles × 3 linguagens = 189 runs*
- *BLOCKER (2026-08-30):* Run completo com MLX falha — stream emite poucos bytes e watchdog aborta. Decisão: instrumentar stream (tese E) e teste diferencial com Colibre AI.

- *Linguagens e bugs de código quebrado:*
  - [ ] Go (=go test= para validar): broker.go com data race, goroutine leak, nil deref
  - [ ] React/TS (=npm test=/eslint): componente com useEffect infinite loop, stale closure, key warning
  - [ ] Python (=pytest=): função O(n²)→O(n), resource leak, wrong exception type

- *Como testar cada combinação (via =magent-cli run=):*
  - [ ] Para cada (modelo, profile, linguagem): copiar =broken-X.ext= para dir temp, rodar =magent-cli run --model "LM Studio Local/<model>" --profile <p> --prompt "Corrija este código"=
  - [ ] Validar: rodar o teste da linguagem (=go test= / =npm test= / =pytest=) após a correção
  - [ ] Medir: acurácia (teste passa?), tempo de resposta, número de tool calls, diff gerado vs esperado
  - [ ] Gravar JSONL/JSON resultados em =/tmp/bench-results/=
  - [ ] Script =bin/bench-code-repair.py= para orquestrar a matriz e sumarizar

*** TODO Testar modelos IBM Granite 4.1 locais (Ollama/MLX) — Mac Mini M2
:PROPERTIES:
:CREATED: 2026-08-24
:STATUS: BACKLOG
:END:

Hardware: Mac Mini M2 / 24 GB — memória unificada CPU-GPU (~15-18 GB úteis para modelos). Ollama v0.32.5 e MLX (porta 8081) já instalados.
Candidatos sweet spot:
- [ ] Granite 4.1 8B (~5.5 GB): linguagem/RAG; substituto direto do =qwen2.5-coder:3b= com qualidade superior.
- [ ] Granite Code 4.1 8B: geração de código enterprise; licença Apache 2.0 IP-safe.
- [ ] Granite Vision 4.1 4B (~3 GB): nova capability — extração de PDFs/tabelas.
- [ ] Granite Speech 4.1 2B Plus (~1.5 GB): nova capability — transcrição local com diarização.
- [ ] Subagente =coder=: testar Granite Code 4.1 8B substituindo Qwen3.5-Coder-7B.
- [ ] Subagente =tech-writer=: testar Granite 4.1 8B substituindo Gemma-4-9B.
- [ ] Subagente =explore=/=qa=: testar Granite 4.1 3B substituindo Qwen2.5-Coder.
- [ ] Capabilities novas: Vision 4B, Guardian 8B (safety), Embedding 278M (RAG local).

** Fase 4 — Integração Magent & Quotas (Complex/High)

- [ ] F4-quota: Monitoramento assíncrono de cotas (OpenCode, Gemini) + injeção no Transient e failover pre-emptivo.
- [ ] F4-proxy: AGY Proxy gptel bridge.
- [ ] F4-toolserver: Tool Server Bridge capstone.

*** TODO Ponte Proxy HTTP Local OpenAI API (AGY CLI Bridge & 3-Tier Fallback)

Servidor Python (=lib/agy_proxy/=), lifecycle elisp e FSM de cota já operam. Integração gptel restante:
- [ ] Registrar backend =gptel-make-openai= AGY Proxy (:8088, modelos =agy-flash=/=agy-pro=) em =custom-ai.el=.
- [ ] Criar =+carlos/ensure-agy-proxy-running= com auto-start reusando o lifecycle de =custom-magent-proxy.el=.
- [ ] Plugear a cascata de resiliência em 3 níveis no =+carlos/gptel-dynamic-router-advice= (Nível 1 AGY Proxy).

*** TODO Monitoramento Proativo de Cotas, Comando /quota e Failover Pre-emptivo (lisp/custom-magent-quota.el)

Estruturas de Dados e Tabela de Estado de Cotas:
- Definir a struct =magent-quota-entry= com os campos correspondentes.
- Criar a hash-table global =+carlos/magent-quota-table=.
- Implementar funcoes de consulta e predicados de saude (=+carlos/magent-quota-healthy-p= com limiar padrao de 5%).

Coletores Assincronos por Provedor:
- Implementar =+carlos/magent-quota-fetch-all= e dispatchers por provedor utilizando requisicoes assincronas (=url-retrieve= / =make-process=).
- Coletar dados de AGY Proxy, Google AI Studio, OpenCode Zen, DeepSeek, Anthropic e Local MLX / Ollama.

Interface do Usuario, Comando Slash /quota e Menu Transient (C-c A m):
- Implementar o comando slash =/quota= para renderizar o buffer temporario =*Magent Quotas*= formatado em tabela Org-mode.
- Integrar o atalho no menu Transient do Magent.
- Adicionar indicador de cota no mood-line do Emacs.

Mecanismo de Failover Pre-emptivo:
- Modificar =+carlos/magent-subagent-resolve= para consultar a saude da cota antes de acionar o LLM.
- Quando a cota for critica, desviar preemptivamente para o proximo nivel da cascata.
- Registrar os eventos de failover pre-emptivo na maquina de estados.

*** TODO Servidor de Ferramentas (Tool Server Bridge)

A inteligência da CLI =agy= externa precisa consumir as ferramentas Lisp nativas do Emacs.
O Proxy resolve isso agindo como ponte de inversão de controle.

*Fluxo FSM Tool Interceptor:*
1. *Discovery*: O proxy expõe as ferramentas nativas (magit, org_smart_edit, elisp_smart_edit, dape) como array de =tools= na API OpenAI-compatible enviada ao Upstream.
2. *Interceptação*: Quando a API retorna =tool_calls=, o FSM intercepta.
3. *Execução Bridge*: O FSM usa =bin/magent-cli= (rodando =emacs --batch -l init.el=) para executar a função de forma idêntica à versão nativa no Emacs.
4. *Resolução*: O FSM do Proxy retorna os =tool_results= ao LLM transparente para o cliente =agy=.

*** TODO Matriz de Roteamento Híbrido e Model Mapping no Proxy
:PROPERTIES:
:CREATED: 2026-08-24
:STATUS: BACKLOG
:END:

Implementação concluída em =lib/agy_proxy/= (=router.py=, =upstream.py=, =config.py= com =model_mapping=). Restante:
- [ ] Testar rotas nas duas máquinas: agnes (validado localmente) e aa102-006l.

*** TODO Roteamento: preferência explícita de backend no select_model + guarda no host-routing
:PROPERTIES:
:CREATED: 2026-08-24
:STATUS: BACKLOG
:END:

Sintoma: usuário pediu explicitamente Google API/Gemini flash e o Magent manteve OpenCode Zen.
- [ ] Adicionar parâmetro =backend-preferred= (e/ou =model=) à tool =select_model= (=+carlos/magent-tool-select-model=, =lisp/custom-magent-tools.el:908=) repassado como =:preferred-backend= aos hints.
- [ ] Guarda em =+carlos/magent-apply-host-routing= (=lisp/custom-magent-fsm.el:402=): registrar erro visível quando =gptel-get-backend= não resolver o backend do perfil do host.
- [ ] Testes ERT dos dois comportamentos.

* Bugs Ativos

** DONE Bug: =magit_stage= via =bin/magent-cli= não persiste staging no git index
:PROPERTIES:
:CREATED:  2026-09-01
:STATUS:   DONE
:END:

Sintoma: =magit_stage TODO.org= via =bin/magent-cli= retornava "Staged file via Magit" (sucesso), mas o staging não persistia para o próximo processo =emacs --batch=.

- *Causa-Raiz Descoberta:* As funções =magit-stage-file= e =magit-stage-all= não existem na API do Magit (retornavam =void-function= / =fboundp nil=). O código executava um bloco silencioso que nada fazia e retornava a string formatada.
- *Correção Aplicada:* Atualizado para =magit-run-git "add" "--" file= e ="-A"= com fallback. No commit, =magit-run-git "commit" -m= foi priorizado para evitar buffers interativos no batch.
- [X] Reproduzir em ambiente controlado (=emacs --batch= isolado).
- [X] Corrigir a tool para garantir persistência do staging.
- [X] Adicionar teste ERT de round-trip: stage + verificar index (=myemacs-magit-tool-stage-persistence=).

* Melhorias UX & UI

** DONE Diagnóstico e Correção — Fechamento Prematuro da GUI no Modo Daemon
:PROPERTIES:
:CREATED: 2026-08-28
:STATUS:  DONE
:END:

No modo daemon (=emacs --daemon= ou serviço systemd), ao abrir cliente gráfico
com =emacsclient -c=, a janela GUI é iniciada e fecha imediatamente.

- *Diagnóstico do Log e Causa-Raiz:*
  - *Limitação PGTK/GTK3 (Wayland):* Build do Emacs com =--with-pgtk= possui limitação inerente no GTK3 onde desconexões de display resultam no encerramento imediato do daemon.
  - *Hook de Frame não protegido em custom-dashboard.el:* A função =+carlos/dashboard-open-on-server-frame= é invocada em =server-after-make-frame-hook= chamando =(dashboard-open)= diretamente sem guarda de exceção.
  - *Variáveis de ambiente de GUI no Systemd:* Daemons iniciados via =systemd --user= podem ser criados antes da sessão gráfica.

- *Plano de Correção:*
  - [X] Envolver =+carlos/dashboard-open-on-server-frame= em =with-demoted-errors= em =custom-dashboard.el=.
  - [X] Adicionar guarda =display-graphic-p= e checagem de frame de servidor antes de renderizar dashboard.
  - [X] Documentar comando =systemctl --user import-environment DISPLAY WAYLAND_DISPLAY XDG_RUNTIME_DIR= para inicialização via systemd.

** TODO Otimizações Nativas de Boot (early-init) — Pendência de Validação Prod
:PROPERTIES:
:CREATED: 2026-08-27
:STATUS:  EM ANDAMENTO
:END:

Cirurgia no =early-init.el= concluída (commit =9a12d69=): =native-comp-compiler-options= por host, GC threshold 100 MiB, =native-comp-async-jobs-number= dinâmico.

- [ ] Pendência de validação: confirmar em prod (=just sync= + =just rebuild-prod=) que o boot acelera e que =just compile-prod= escala em 8 cores sem warning.

** TODO Estilização e Alinhamento do Minibuffer (Padrões Protesilaos)

- [ ] Pesquisar e prototipar propostas baseadas na filosofia do Protesilaos (Prot) para o Minibuffer. Avaliar o pacote =mct= ou integrações visuais do =ef-themes= / =standard-themes=.
- [ ] Resolver problemas de alinhamento e legibilidade do minibuffer atual (=vertico= + =marginalia= + =corfu= + =orderless=).
- [ ] Corrigir Roteamento de Aprovação de Subagentes: alterar =+carlos/magent-ui--approval-route= para rastrear a sessão "pai" (orquestrador) e injetar botão de Allow/Deny no buffer principal do chat.

** TODO Cross-Streaming UI (Pensamentos do Subagente no Orquestrador)

- [ ] Implementar leitura dinâmica no =custom-magent-ui.el=. Quando a FSM do Orquestrador estiver no estado =subagent-waiting=, o spinner deve ler as últimas linhas do =session.json= do subagente e transmitir o texto de raciocínio em tempo real.

* Infraestrutura Avançada

** TODO Structured Outputs e Eliminação de Alucinações (Fase 7)
:PROPERTIES:
:CREATED: 2026-08-23
:STATUS: BACKLOG
:END:

- [ ] *Gerador Dinâmico de Schema no Elisp:* Compilar configurações das ferramentas ativas e gerar JSON Schema com campo ="rationale"= obrigatório (CoT embutido no JSON).
- [ ] *Interceptador (Advice) no Backend HTTP do Gptel:* Injetar =response_format= no payload HTTP quando =magent-fsm-mode= estiver ativo e o modelo não for de raciocínio (DeepSeek R1).
- [ ] *Validação Prática com Modelo Minúsculo:* Testar FSM com =Qwen2.5-Coder-1.5B= no MLX para provar que restrição de logits força conformidade JSON. Benchmark no host =aa102-006l= com Ollama (GBNF).

** TODO Integração de JSON Schema e GBNF no Emacs (MLX/Ollama)
:PROPERTIES:
:STATUS: BACKLOG
:END:

- =gptel=: Usar o argumento =:schema= na API de requisições (=gptel-request=).
- =ellama= / =llm.el=: Suporta =:response-format= usando JSON Schema.
- =GBNF Puro=: Extender requisição HTTP para o endpoint =/completion= com gramática.

* Backlog

** TODO Conscientização de Contexto Espacial em Submódulos

- [ ] Criar regra estrita nos perfis de RAG (ou no =context_search=) para injetar avisos quando a pasta de trabalho for um git submodule.

** TODO Integrar =+carlos/magent-lint-defun-parens= como gate extra no =elisp-smart-edit=

- [ ] Rodar antes de =check-parens= para pegar off-by-one mais cedo (pendência da integração de linters nativos).

** TODO Atualizar =docs/dev-workflow.org= com a nova pipeline de validação

- [ ] Documentar =just lint= completo (compile + checkdoc + org-lint + lint-native).

** TODO Ferramental de Diagnóstico & Introspecção Nativa (Magent CLI)
:PROPERTIES:
:CREATED: 2026-09-03
:STATUS:  EM ANDAMENTO
:END:

Identificado durante resolução de bugs em modo batch. Ferramentas nativas de diagnóstico para acelerar a depuração de agentes e CLIs externas:
- [X] *Fase 1 (Imediato — Force Multiplier para Bug 2 e 4):*
  - [X] Implementar =elisp_eval= (com macroexpand e captura de stdout/erro) em =custom-magent-tools.el=.
  - [X] Atualizar docstring de =elisp_smart_edit= oficializando a ação =replace_text=.
- [ ] *Fase 2 (Backlog):*
  - [ ] =elisp_trace=: Ativar =trace-function= em funções-alvo e retornar o buffer =*trace-output*= como string/JSON.
  - [ ] =git_status_porcelain=: Tool leve de Git para obter o estado do index e worktree sem instanciar os buffers pesados do Magit.

* Contexto de Arquitetura Oficial do Magent (Referência RAG)

Síntese da documentação oficial do repositório Magent (=docs/AGENT_JOBS.org=, =docs/AGENT_WORKFLOW.org=, =docs/ONBOARDING.org=):

- *Modelo de Subagentes (AGENT_JOBS.org):* Jobs duráveis com a struct =magent-agent-job= (=queued=, =running=, =waiting=, =completed=, =failed=, =closed=, =cancelled=). O =wait_agent= assina observadores de transição com deadline timer (sem polling). Herança de contexto delimitada por =magent-child-agent-max-depth= (padrão 1 nível).
- *Ledger & State Machine (AGENT_WORKFLOW.org):* Hierarquia Codex =thread -> turn -> item=. Turno gerenciado em =magent-ledger.el=. Itens de ferramenta unificados (=tool-call= e =tool-output= no mesmo registro por =call-id=). Persistência dupla via =Snapshot= (restauração rápida de thread) + =Journal= (log append-only).
- *Camadas de Arquitetura (ONBOARDING.org):* 7 camadas. Camada 2 (Sessão & Ledger), Camada 3 (Agentes e Permissões =allow=/=deny=/=ask=), Camada 4 (15 ferramentas nativas em =magent-tools.el= + aprovações em =magent-approval.el=), Camada 5 (Loop Magent e transport fino =gptel-request= em =magent-llm-gptel.el=), Camada 6 (Frontend =agent-shell= + protocolo ACP em =magent-acp.el=).

* Concluído (referências ao roadmap)

- O histórico completo de todas as tarefas concluídas é mantido estritamente em [[file:roadmap.org][roadmap.org]] como Linha do Tempo oficial.
- *2026-08-25:* Phase A — toggle_checkbox + lint-native no pipeline → commit 606c47c → [[file:roadmap.org][roadmap.org]].
- *2026-08-25:* Phase B — +carlos/magent-project-root utility, 31 call sites → commit bc763fd → [[file:roadmap.org][roadmap.org]].
- *2026-08-25:* P3 dry-run guards (branch-delete, docker-action, systemd-action) → [[file:roadmap.org][roadmap.org]].
- *2026-08-25:* Linters nativos (defun-parens + arity) + helpers portáteis → [[file:roadmap.org][roadmap.org]].
- *2026-08-27:* Correção de Hangs Críticos e Timers no magent-cli (--batch) → [[file:roadmap.org][roadmap.org]].
- *2026-09-01:* Eshell override =git log= → buffer Magit; prompt devenv ativo; popper aside lateral esquerdo → custom-term.el + docs/term-stack.org.
- *2026-09-01:* Migração de 8 tarefas DONE para [[file:roadmap.org][roadmap.org]] (F6-3b, toggle_checkbox, project-root, linters, org_smart_edit AST, magent-cli auto-descritiva, SOLID Fase 5, CB async fix, regressão gptel-backend).
