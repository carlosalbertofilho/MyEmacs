#+TITLE: TODO — Planejamento Ativo e Backlog (MyEmacs)
#+AUTHOR: Carlos Filho
#+DATE: 2026-08-05
#+LAST_MODIFIED: 2026-08-20
#+FILETAGS: :PLANNING:
#+OPTIONS: toc:2 num:t
#+STARTUP: content
#+TODO: TODO(t) | DONE(d) CANCELLED(c) BLOCKED(b)

* Visão Geral

Artefato único de planejamento (AGENTS.md). Apenas pendências ativas.
Histórico consolidado no [[file:roadmap.org][roadmap.org]].

- *North Star:* Magent/gptel como driver do Emacs.
- *Etapas:* A (Fundação) ✅ → B (Motor) ✅ → C (Ambição) ✅.
- *Últimos fixes:* Fix Subagentes Sem Tools (2026-08-20), Tier Escalation (2026-08-20).
- *Suíte:* 375 testes ERT (365 passed, 0 failures, 10 skipped).

* Contexto de Arquitetura Oficial do Magent (Referência RAG)

Síntese da documentação oficial do repositório Magent (=docs/AGENT_JOBS.org=, =docs/AGENT_WORKFLOW.org=, =docs/ONBOARDING.org=):

- *Modelo de Subagentes (AGENT_JOBS.org):* Jobs duráveis com a struct =magent-agent-job= (=queued=, =running=, =waiting=, =completed=, =failed=, =closed=, =cancelled=). O =wait_agent= assina observadores de transição com deadline timer (sem polling). Herança de contexto delimitada por =magent-child-agent-max-depth= (padrão 1 nível).
- *Ledger & State Machine (AGENT_WORKFLOW.org):* Hierarquia Codex =thread -> turn -> item=. Turno gerenciado em =magent-ledger.el=. Itens de ferramenta unificados (=tool-call= e =tool-output= no mesmo registro por =call-id=). Persistência dupla via =Snapshot= (restauração rápida de thread) + =Journal= (log append-only).
- *Camadas de Arquitetura (ONBOARDING.org):* 7 camadas. Camada 2 (Sessão & Ledger), Camada 3 (Agentes e Permissões =allow=/=deny=/=ask=), Camada 4 (15 ferramentas nativas em =magent-tools.el= + aprovações em =magent-approval.el=), Camada 5 (Loop Magent e transport fino =gptel-request= em =magent-llm-gptel.el=), Camada 6 (Frontend =agent-shell= + protocolo ACP em =magent-acp.el=).

* Pendente — Motor & Backlog (Plano Mestre Opus)

** TODO Resiliencia e Robustez FSM
    :PROPERTIES:
    :CREATED: 2026-08-20
    :ASSIGNEE: coder
    :STATUS: plano aprovado
    :END:

*** Circuit Breaker de nuvem
Implementar bloqueio preventivo (circuit breaker) em =custom-magent-tools.el= contra APIs e LLMs instáveis.

*** Sanitizacao ANSI e XML
Aplicar filtro sanitizador para limpar saídas ANSI/XML quebradas em =+carlos/magent-tool-result=, assegurando que o seletor =:call-id= não sofra corrupção sintática.

*** Otimizacao de turnos e bloqueios
Programar threshold de GC dinâmico durante turnos ativos da FSM em =custom-magent-fsm.el= e auto-resync para estados de =buffer_conflict=.

*Arquivos:* =lisp/custom-magent-fsm.el=, =lisp/custom-magent-tools.el=, =lisp/custom-magent-buffer.el=.

** TODO Teste de Delegação (12 rounds de regressão)
    :PROPERTIES:
    :CREATED: 2026-08-19
    :ASSIGNEE: qa
    :STATUS: plano aprovado
    :END:

Matriz executável dos 12 rounds de regressão do Magent.

| Round | Prompt de Teste                                           | Perfil Esperado         | Tools Esperadas                               | Status |
|-------+-----------------------------------------------------------+-------------------------+-----------------------------------------------+--------|
|     1 | Encontre a definição de =+carlos/magent-subagent-profile= | =explore= / =general=   | =grep_search=, =read_file=                    | TODO   |
|     2 | Quais pacotes são carregados com =:demand t= em init.el?  | =explore=               | =read_file=                                   | TODO   |
|     3 | analise custom-magent-fsm.el e descreva FSM               | =tech-writer=/=planner= | =read_file=                                   | PASSED |
|     4 | Altere referências de 'old-var' para 'new-var' em scratch | =coder=                 | =buffer_replace=, =edit_file=                 | TODO   |
|     5 | Mova função X de file-a.el para file-b.el                 | =coder=                 | =read_file=, =edit_file=, =flycheck_errors=   | TODO   |
|     6 | adicione '; TODO: review' no topo de custom-ui.el         | =coder= / =general=     | =edit_file=                                   | PASSED |
|     7 | Crie um card 'Bateria 12 Rounds' no TODO.org sob Pendente | =planner=               | =read_file=, =edit_file=, =org-lint=          | TODO   |
|     8 | Resuma status dos bugs resolvidos em AGENTS.md            | =qa= / =tech-writer=    | =read_file=                                   | TODO   |
|     9 | Crie um arquivo denote documentando a nova FSM            | =tech-writer=           | =run_command= / tool denote                   | TODO   |
|    10 | Qual é o status git atual? Existem arquivos unstaged?     | =sysadmin= / =coder=    | =run_command= (=git status=)                  | TODO   |
|    11 | Rode o just compile e me diga se há warnings              | =qa=                    | =run_command= (=just compile=)                | TODO   |
|    12 | Execute a suíte test-ai e relate as falhas, se houver     | =qa=                    | =run_command= (=just test-ai=)                | TODO   |
** TODO Contornar instabilidade de Function Calling no OpenCode Zen (gpt-5.6-sol)
    :PROPERTIES:
    :CREATED: 2026-08-22
    :ASSIGNEE: coder
    :STATUS: BACKLOG
    :END:

*** Diagnóstico e Solução
O modelo =gpt-5.6-sol= (via OpenCode Zen) gera tool calls mal formatados (=name: "unknown"=, =args-json: "null"=), quebrando a FSM com =Error: Could not parse HTTP response.=.
Devemos ajustar =custom-ai.el= ou =custom-magent-subagent.el= para contornar ou isolar esse modelo.

*Arquivos:* =lisp/custom-ai.el=, =lisp/custom-magent-subagent.el=.

* Concluído (referências ao roadmap)

- *Normas IETF e Extensões (rfc-mode)* (2026-08-22) — `use-package rfc-mode` em `custom-files.el` (cache em `~/.config/emacs/rfc/`), ferramentas `rfc_search_topic` / `rfc_read_section` registradas em `custom-magent-tools.el` e permissões `allow` atribuídas aos perfis `auditor` e `sec-ops` em `custom-magent-team.el`.
- *Otimização do Factory Reset & Build* (2026-08-22) — `install-prod` no Justfile, `elpaca-queue-limit` dinâmico por CPUs, `history` ignorado e removido do git tracking. Testado e validado com sucesso em 12 cores no host `aa102-006l`.
- *Subagentes Sem Tools* (2026-08-20) — Separação de diretivas em 3 constantes e injeção por papel. Log verificado com 100% de sucesso.
- *Tier Escalation* (2026-08-20) — =select_model= aceita =min_tier=. 2 testes ERT.
- *Fix Subagente Órfão* (2026-08-20) — Observer 1-arg, auto-resume, spinner. 11 testes ERT.
- *Reescrita Diretiva #3 + Buffer Save* (2026-08-20) — DELEGATION FIRST.
